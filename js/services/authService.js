/**
 * @module services/authService
 * handles authentication of user logins.  
 * Makes sure that entered passwords match the one stored
 * to the database for a given account.
 */
// user approval


// user management


// admin-only overrides for other controllers like fileController

// access control